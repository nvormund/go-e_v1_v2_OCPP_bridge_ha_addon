# go-e v1_v2 OCPP Bridge

Home Assistant App fuer die go-e OCPP Bridge.

Die App laeuft isoliert als Container und verbindet einen go-eCharger per lokaler HTTP API mit einem OCPP-1.6-JSON-Backend.

Bitte zuerst mit `dry_run: true` und `read_only: true` starten.
